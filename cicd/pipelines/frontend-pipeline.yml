name: Frontend CI/CD

on:
  push:
    branches: [ main ]
    paths:
      - 'frontend/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'frontend/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        app: [shell, task-management-app, result-viewer-app]
    
    steps:
    - uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '16'
        
    - name: Install dependencies
      run: |
        cd frontend/${{ matrix.app }}
        npm ci
        
    - name: Lint
      run: |
        cd frontend/${{ matrix.app }}
        npm run lint
        
    - name: Test
      run: |
        cd frontend/${{ matrix.app }}
        npm test
        
    - name: Build
      run: |
        cd frontend/${{ matrix.app }}
        npm run build
        
    - name: Upload build artifacts
      uses: actions/upload-artifact@v3
      with:
        name: ${{ matrix.app }}-build
        path: frontend/${{ matrix.app }}/dist
        
  docker-build:
    needs: build-and-test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        app: [shell, task-management-app, result-viewer-app]
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Download build artifacts
      uses: actions/download-artifact@v3
      with:
        name: ${{ matrix.app }}-build
        path: frontend/${{ matrix.app }}/dist
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
      
    - name: Login to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
        
    - name: Build and push
      uses: docker/build-push-action@v4
      with:
        context: ./frontend/${{ matrix.app }}
        push: true
        tags: yourusername/local-ai-agent-${{ matrix.app }}:latest
        
  deploy:
    needs: docker-build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Deploy to server
      run: |
        echo "Deploying frontend applications..."
        # Add deployment commands here
        # This could be SSH into a server and running docker-compose
        # or updating a Kubernetes deployment
